# TaylorSeries
I wrote a Python script that simplifies complex functions using Taylor series at x=0. It supports sin(x) and e^x. I also custom-coded my own pow() and factorial functions. The simplification runs iteratively until the absolute error drops below a predefined threshold.
